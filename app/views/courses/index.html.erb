





<section class="bg-primary" id="about">
<main role="main" class="container">
      <div class="jumbotron">


<center><h1>COURSES</h1></center>
<br />
<table class="ui selectable celled table">
  <thead class="full-width">
    <tr>
    <th>#</th>
    <th>Name</th>
    <th>Time</th>
    <th>Delete</th>
    </tr>
  </thead>
  <tbody>
  <% @courses.each_with_index do |course, index| %>
    <tr id="course-<%= course.id %>"> 
      <td><%=index+1%></td>
      <td><%=link_to course.name, course_path(course)%></td>
      <td><%=course.time.to_s%></td>
      <td> <%= link_to 'Remove', course_path(course), method: :delete, remote:true, data: { confirm: 'Are you sure?' } %>
      </td>
    </tr>
    <% end%>
  </tbody>
  <tfoot class="full-width">
    <tr>
      <th></th>
      <th colspan="6">
          <%=link_to "Add course", new_course_path(@course), :class=> "ui inverted orange button", data: { turbolinks: false }%>
          
        </div>  
      </th>
    </tr>
  </tfoot>
</table>


</div>
    </main>
    </section>




